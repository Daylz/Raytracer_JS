class RTSphere {
    constructor(position = createVector(0, 0, 0), radius = 0, color = color(255, 255, 255)) {
        this.position = position;
        this.radius = radius;
        this.color = color;
    }
}